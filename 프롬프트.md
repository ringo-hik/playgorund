# AI ChatOps 프로젝트 변경 내역

## 주요 변경 사항

### UI/UX 개선
- 플로팅 챗봇의 위치 및 동작 방식 개선
- 버튼 스타일 통일 및 디자인 개선
- 카테고리 및 페르소나 선택 카드 UI/UX 개선
- 전반적인 디자인 시스템 개선 및 시각적 효과 적용
  - `customMarkdown.css`에서 `clamp()` 함수를 활용하여 반응형 폰트 크기를 적용하고, `--font-family`, `--color-text-primary` 등 메인 CSS 변수를 사용하여 일관된 디자인 시스템을 구축했습니다.
  - 링크(`markdown-link`) 및 헤딩(`markdown-h1`, `markdown-h2` 등)에 대한 상세 스타일링(밑줄, 호버 효과, 시각적 강조)을 통해 사용자 경험을 향상시켰습니다.

### 접근성 향상
- WCAG 2.1 AA 표준을 준수하는 색상 시스템 도입 (15가지 조합)
  - `customMarkdown.css`에서 `--color-info`, `--color-success`, `--color-warning`, `--color-error` 등 의미론적 색상 변수를 정의하여 WCAG 2.1 AA 표준을 준수하는 색상 팔레트를 구현했습니다. 이를 통해 다크/라이트 모드 전환 시에도 높은 가독성과 접근성을 보장합니다.
- 모든 사용자가 원활하게 서비스를 이용할 수 있도록 접근성 강화

### 테마 및 스타일링
- 기존 테마 시스템 복구 및 안정화
- CSS 구조 최적화 및 불필요한 스타일 제거
  - `customMarkdown.css`에서 `--md-*` 변수를 제거하고 메인 CSS 변수를 직접 사용하여 CSS 구조를 간소화하고 유지보수성을 높였습니다.
- 다크 모드/라이트 모드 지원 강화
  - `--motion-fast`와 같은 CSS 변수를 활용하여 부드러운 UI 전환 효과를 제공하며, `@media (max-width: 640px)` 미디어 쿼리를 통해 모바일 환경에서의 반응형 디자인을 강화했습니다.

### 기능 이전 및 통합
- 주요 UI/UX 개선 사항을 `vue-aiops-real` 프로젝트로 성공적으로 이전
- `AIOpsChat`에서 `aiOps`로 디렉토리 구조 변경 및 표준화

### 테스트 및 안정성
- Playwright를 활용한 E2E (End-to-End) 테스트 도입
- 테스트 자동화를 통해 주요 기능의 안정성 확보
  - `aiChatOpsService.js` 내의 API 호출 로직에 `console.log`를 통한 상세 로깅을 추가하여 개발 및 디버깅 시 API 상호작용을 명확하게 추적할 수 있도록 했습니다.
- 테스트 결과 리포트 생성 및 관리
  - `aiChatOpsService.js`의 `getErrorMessage` 유틸리티 함수는 HTTP 상태 코드 및 네트워크 오류에 대한 구체적인 에러 메시지를 제공하여 안정적인 오류 처리를 지원합니다.

### 코드 리팩토링 및 버그 수정
- Vue 컴포넌트 구조 개선 및 ES6 모듈 임포트 방식 통일
- Unicons 아이콘 라이브러리 의존성 문제 해결
  - `aiChatOpsService.js`의 `getPersonaIcon` 및 `getRandomPersonaIcon` 함수를 통해 페르소나 코드에 따른 아이콘을 동적으로 관리하고, 아이콘 라이브러리 의존성 문제를 해결했습니다.
- 기타 자잘한 버그 수정 및 코드 품질 개선
  - `aiChatOpsService.js`의 `htmlToMarkdown` 및 `htmlToPlainText` 함수는 HTML 콘텐츠를 마크다운 또는 일반 텍스트로 변환하는 강력한 기능을 제공하며, 테이블, 헤딩, 코드 블록 등 다양한 HTML 요소를 정확하게 처리합니다. `stripHtml` 함수는 HTML 태그를 제거하여 클린 텍스트를 추출하는 데 사용됩니다.

## 개발 워크플로우
- AI 코드 생성 도구(Claude)를 적극적으로 활용하여 개발 생산성 향상
